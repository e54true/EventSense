{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "_comment": "Healthcheck is NOT set here — only backend service serves HTTP. Worker/analyzer/beat run Celery and have no /health endpoint, so a shared healthcheck path would fail their deploys. backend service sets healthcheckPath via Railway UI.",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
